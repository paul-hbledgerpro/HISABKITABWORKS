{
  "name": "hisab-kitab-bank-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply hisab-kitab-bank-sync --local",
    "db:migrate:remote": "wrangler d1 migrations apply hisab-kitab-bank-sync --remote"
  },
  "dependencies": {
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.112.0"
  }
}
