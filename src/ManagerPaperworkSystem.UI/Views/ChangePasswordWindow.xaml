<Window Icon="pack://application:,,,/Assets/HisabKitab.ico" x:Class="ManagerPaperworkSystem.UI.Views.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HISAB KITAB - Change Password" Height="340" Width="460" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}" FontFamily="Segoe UI">

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Change Password" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

    <TextBlock Grid.Row="1" Text="New Password"/>
    <PasswordBox Grid.Row="2" x:Name="pwd1"/>

    <TextBlock Grid.Row="3" Text="Confirm Password"/>
    <PasswordBox Grid.Row="4" x:Name="pwd2"/>

    <DockPanel Grid.Row="5" LastChildFill="False" Margin="0,10,0,0">
      <TextBlock x:Name="lblError" Foreground="#FFFF6B6B" TextWrapping="Wrap" DockPanel.Dock="Left"/>
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
        <Button Content="Cancel" Width="110" Margin="0,0,10,0" Click="Cancel_Click"/>
        <Button Content="Save" Width="110" Click="Save_Click"/>
      </StackPanel>
    </DockPanel>
  </Grid>
</Window>
