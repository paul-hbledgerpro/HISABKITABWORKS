<Window Icon="pack://application:,,,/Assets/HisabKitab.ico" x:Class="ManagerPaperworkSystem.UI.Views.ScreenSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HISAB KITAB - Screen Mode" Height="240" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="{StaticResource BgBrush}" Foreground="{StaticResource TextBrush}" FontFamily="Segoe UI">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Select Screen Mode" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>

    <ComboBox Grid.Row="1" x:Name="cmb"/>

    <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,12,0,0">
      <TextBlock x:Name="lbl" Foreground="#FFFF6B6B" DockPanel.Dock="Left"/>
      <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
        <Button Content="Cancel" Width="110" Margin="0,0,10,0" Click="Cancel_Click"/>
        <Button Content="Save" Width="110" Click="Save_Click"/>
      </StackPanel>
    </DockPanel>
  </Grid>
</Window>
